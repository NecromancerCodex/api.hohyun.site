# Nginx 리버스 프록시 설정
# EC2 인스턴스에 설치하여 Vercel과 API Gateway 연결

# HTTP → HTTPS 리다이렉트 (선택사항)
server {
    listen 80;
    server_name api.hohyun.site www.hohyun.site admin.hohyun.site;
    
    # Let's Encrypt 인증을 위한 ACME 챌린지
    location /.well-known/acme-challenge/ {
        root /var/www/html;
    }
    
    # HTTPS로 리다이렉트 (SSL 인증서 설정 후 활성화)
    # return 301 https://$server_name$request_uri;
    
    # 임시로 HTTP 허용 (SSL 설정 전까지)
    location / {
        proxy_pass http://localhost:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

# HTTPS 설정 (SSL 인증서 설정 후 활성화)
# server {
#     listen 443 ssl http2;
#     server_name api.hohyun.site www.hohyun.site admin.hohyun.site;
#     
#     # SSL 인증서 경로 (Let's Encrypt 사용 시)
#     ssl_certificate /etc/letsencrypt/live/api.hohyun.site/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/api.hohyun.site/privkey.pem;
#     
#     # SSL 설정
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#     
#     # API Gateway로 프록시
#     location / {
#         proxy_pass http://localhost:8080;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#         
#         # 타임아웃 설정
#         proxy_connect_timeout 60s;
#         proxy_send_timeout 60s;
#         proxy_read_timeout 60s;
#     }
#     
#     # Health Check 엔드포인트
#     location /actuator/health {
#         proxy_pass http://localhost:8080/actuator/health;
#         access_log off;
#     }
# }

# API Gateway 전용 설정 (api.hohyun.site)
server {
    listen 80;
    server_name api.hohyun.site;
    
    # CORS 헤더 추가 (필요시)
    add_header 'Access-Control-Allow-Origin' '*' always;
    add_header 'Access-Control-Allow-Methods' 'GET, POST, PUT, DELETE, OPTIONS' always;
    add_header 'Access-Control-Allow-Headers' 'Authorization, Content-Type' always;
    
    # OPTIONS 요청 처리
    if ($request_method = 'OPTIONS') {
        return 204;
    }
    
    # API Gateway로 프록시
    location / {
        proxy_pass http://localhost:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # 타임아웃 설정
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
}

